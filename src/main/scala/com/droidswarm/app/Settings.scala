package com.droidswarm.app

object Settings {

  def trailLength = 25

  val minRotation = (10 / 180) * Math.Pi
  val maxRotation = (20 / 180) * Math.Pi

  def worldSizeX = 100
  def worldSizeY = 100
}